diff a/Jenkinsfile b/Jenkinsfile	(rejected hunks)
@@ -12,13 +12,7 @@
     }
 
     stages {
-        stage('Récupération du code') {
-            steps {
-                echo '📥 Clonage du dépôt GitHub...'
-                git credentialsId: "${GIT_CREDENTIALS_ID}", url: "${GIT_REPO_URL}", branch: "${GIT_BRANCH}"
-            }
-        }
-
+        
         stage('Nettoyage du dossier target avec Maven') {
             steps {
                 echo '🧹 Suppression du dossier target via mvn clean...'
